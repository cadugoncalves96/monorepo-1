#!/usr/bin/env groovy

def projectDir = '/var/jenkins_home/master/play-b'

pipeline {
    agent any
    options {
        skipDefaultCheckout true
    }
    stages {
        stage('init') {
            steps {
                echo "começo de build do play b"

                }
            }
        }
    }
}
