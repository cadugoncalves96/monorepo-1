#!/usr/bin/env groovy

def projectDir = '/var/lib/jenkins/master/play-b'

pipeline {
    agent any
    options {
        skipDefaultCheckout true
    }
    stages {
        stage('init') {
            steps {
                echo "começo de build do play b"

                }
            }
        }
    }
}
